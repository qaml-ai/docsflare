---
title: MDX Components
description: Supported Mintlify-style MDX components.
---

# MDX Components

Docsflare supports standard Markdown, GitHub-flavored Markdown, frontmatter, and common Mintlify-style MDX components.

<Info>
MDX is rendered at build time, so pages are fast and portable across supported runtimes.
</Info>

## Cards

<CardGroup cols={2}>
  <Card title="Quickstart" href="/quickstart">
    Get the docs application running locally.
  </Card>
  <Card title="Search" href="/cloudflare-search">
    Configure Cloudflare AI Search.
  </Card>
</CardGroup>

## Tabs

<Tabs>
  <Tab title="npm">
    ```bash
    npm run dev
    ```
  </Tab>
  <Tab title="deploy">
    ```bash
    docsflare deploy docs
    ```
  </Tab>
</Tabs>

Tabs and code groups are interactive, keyboard accessible, and collapse to a single column on small screens.

<CodeGroup>
```typescript TypeScript
const docs = "fast";
```

```python Python
docs = "fast"
```
</CodeGroup>

## Accordions

<Accordion title="Can I use normal Markdown?">
Yes. Tables, task lists, fenced code blocks, and frontmatter are supported.
</Accordion>

<Expandable title="advanced options">
Expandable content works well for nested API properties and secondary detail.
</Expandable>

## Callouts and emphasis

<Tip>Use callouts sparingly so important guidance stays noticeable.</Tip>
<Warning>Warnings and danger callouts have distinct accessible color treatments.</Warning>
<Danger>Danger highlights destructive or irreversible actions.</Danger>

Inline status works with <Badge color="#059669">Stable</Badge>, and <Tooltip tip="Extra context without interrupting the page">tooltips</Tooltip> keep details close at hand.

## Layout

<Columns cols={2}>
  <Column>
    **Flexible columns**

    Columns can contain arbitrary Markdown, not only cards.
  </Column>
  <Panel title="Supplementary detail" icon="info">
    Panels visually separate supporting content from the main narrative.
  </Panel>
</Columns>

<Frame caption="Frames support optional captions.">
  <div style={{ padding: "2rem", textAlign: "center" }}>Image or embedded content</div>
</Frame>

## API documentation

<ParamField query="limit" type="number" default={20}>
  Maximum number of records to return.
</ParamField>

<ResponseField name="items" type="object[]" required>
  The records returned by the API.
</ResponseField>

<RequestExample>
```bash cURL
curl https://api.example.com/items?limit=20
```
</RequestExample>

## Navigation and structure

<TileGroup cols={3}>
  <Tile title="Quickstart" icon="rocket" href="/quickstart" />
  <Tile title="Search" icon="globe" href="/cloudflare-search" />
  <Tile title="Components" icon="code" href="/mdx-components" />
</TileGroup>

<Tree>
  <TreeFolder name="docs">
    <TreeFile name="docs.json" />
    <TreeFile name="quickstart.mdx" />
  </TreeFolder>
</Tree>
